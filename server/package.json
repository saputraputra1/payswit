{
  "name": "payswit-server",
  "version": "1.0.0",
  "description": "Payswit API Server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "docker:build": "docker build -t payswit-server .",
    "docker:run": "docker run -p 5000:5000 --env-file .env payswit-server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^11.11.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
